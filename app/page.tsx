export default function HomePage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-background text-text">
      <div className="max-w-2xl rounded-[28px] border border-border bg-white p-10 shadow-card">
        <h1 className="text-3xl font-semibold">Realty Assistant</h1>
        <p className="mt-3 text-muted">UI preview — full dashboard is loading. If you see this, the build succeeded.</p>
      </div>
    </div>
  );
}
