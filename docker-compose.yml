services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${HOST_PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app:cached
      - /app/node_modules
    command: npm run dev
    restart: unless-stopped
